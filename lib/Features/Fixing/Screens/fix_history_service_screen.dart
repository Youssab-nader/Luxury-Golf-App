import 'package:flutter/material.dart';

class FixHistory extends StatelessWidget {
  const FixHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Welcome Fix History Screen'),),);
  }
}