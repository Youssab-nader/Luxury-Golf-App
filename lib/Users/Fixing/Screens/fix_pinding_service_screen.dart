import 'package:flutter/material.dart';

class FixPindingServices extends StatelessWidget {
  const FixPindingServices({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Welcome Fix Pinding Screen'),),);
  }
}